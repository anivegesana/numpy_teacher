import numpy as np

l = np.array([i for i in range(8)])
ii = range(8)
l = np.array([i for i in ii if i != 2])
l = np.array([0 for i in range(10)])


l = np.array([0] * 10)
l = np.array([2 ** 3] * 20)
